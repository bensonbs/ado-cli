# Required
ADO_PAT=your-personal-access-token-here

# Optional defaults (can be overridden with --org / --project / --area flags)
ADO_ORG=your-organization
ADO_PROJECT=your-project-name
# ADO_AREA_PATH=your-area-path  # defaults to ADO_PROJECT if not set
